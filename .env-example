# DBT Configuration Variables
# Copy this file to .env and fill in the values (if needed)

# Para desenvolvimento local, configure o diretório onde está o profiles.yml
# Se o profiles.yml está na raiz do projeto, use:
# DBT_PROFILES_DIR=/caminho/absoluto/para/raiz/do/projeto
# 
# Ou simplesmente exporte no terminal:
# export DBT_PROFILES_DIR=$(pwd)

# Note: In production (Cloud Run Jobs), authentication is handled automatically
# through the service account configured at the Cloud Run Job level.
# No keyfile path is needed as it uses Application Default Credentials.
